export enum COMPETITION_TYPE {
    GROUP = 'group',
    PERSONAL = 'personal'
}

export enum COMPETITION_STATUS {
    PENDING = 'pending',
    ONGOING = 'ongoing',
    ENDED = 'ended'
}

export enum COMPETITION_ENTRY {
    OPEN = 'open',
    CLOSED = 'closed'
}

export enum COMPETITION_VISIBILITY {
    PUBLIC = 'public',
    PRIVATE = 'private'
}