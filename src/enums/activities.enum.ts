export enum ACTIVITY_ENTITY {
    USER = 'user',
    COMPETITION = 'competition',
    REFERRAL = 'referral'
}

export enum ACTIVITY_MESSAGES {
    CREATED_COMPETITION = 'created a competition',
    JOINED_COMPETITION = 'joined competition',
    CREATED_FORUM = 'Created a new forum'
}